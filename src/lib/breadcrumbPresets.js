/**
 * Reusable first segment for inner pages under the Endorsements module (`/`).
 * Landing page (`EndorsementsDashboard`) passes `breadcrumbs={[]}` — no trail.
 */
export const endorsementsModuleCrumb = { label: 'Endorsements', path: '/' }

/** First segment for inner pages under Claims (`/claims`). */
export const claimsModuleCrumb = { label: 'Claims', path: '/claims' }
